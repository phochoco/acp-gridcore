# Nginx 설정 추가 내용
# 기존 trinity-protocol.com 서버 블록 안에 아래 location 블록 추가

# location /oracle/ {
#     proxy_pass http://127.0.0.1:8001/oracle/;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_read_timeout 60s;
# }

# VPS 배포 명령어:
# 1. git pull
# 2. pip install httpx  (oracle_server.py 의존성)
# 3. sudo cp trinity-oracle.service /etc/systemd/system/
# 4. sudo systemctl daemon-reload
# 5. sudo systemctl enable trinity-oracle
# 6. sudo systemctl start trinity-oracle
# 7. sudo nano /etc/nginx/sites-available/trinity  # location 블록 추가
# 8. sudo nginx -t && sudo systemctl reload nginx
